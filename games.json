function makeDefaultBlockCode(json_data) {
  // Generates a default block code string from the given JSON data.

  // Args:
  //   json_data: A JSON object.

  // Returns:
  //   A JSON object.

  return json_data;
}

function loopJsonContentsAndMakeStringWithDefaultBlockCode(json_contents) {
  // Loops through the given JSON contents and makes a string with the default
  // block code for each item, replacing the image URL, URL, and onclick to what
  // the JSON is.

  // Args:
  //   json_contents: A list of JSON objects.

  // Returns:
  //   A list of JSON objects.

  return json_contents;
}

// Example usage:

const json_contents = [
  {
    "swiper-slide": {
      "box": {
        "box-text": {
          "img": {
            "src": "images/1C15ABFC-CB62-4115-B050-D693649AE1EC.jpeg",
            "alt": "Pacman"
          },
          "h2": "Pacman",
          "h3": "Action",
          "a": {
            "href": "./iframe.html",
            "onclick": "localStorage.setItem('iframeurl', 'embed.html#https://cyamanz.github.io/play/flash/?swf=pacman.swf'); localStorage.setItem('staturl', './iframe.html')",
            "class": "box-btn",
            "i": {
              "class": "fa-solid fa-download"
            }
          }
        }
      }
    }
  }
];

const default_block_code_json = loopJsonContentsAndMakeStringWithDefaultBlockCode(json_contents);

console.log(default_block_code_json);

